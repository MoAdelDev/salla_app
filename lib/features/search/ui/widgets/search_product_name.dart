import 'package:flutter/material.dart';

class SearchProductName extends StatelessWidget {
  const SearchProductName({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
